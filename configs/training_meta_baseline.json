{
  "run_name": "baseline_chunk50_20k",
  "model": "lerobot/smolvla_base",
  "dataset": "HuggingFaceVLA/libero",
  "task": "libero_spatial",
  "steps": 20000,
  "batch_size": 2,
  "seed": 42,
  "eval_freq": 0,
  "save_freq": 5000,
  "use_amp": true,
  "chunk_size": 50,
  "num_denoising_steps": 10,
  "gpu": "Tesla T4",
  "gpu_memory_gb": 14.6,
  "training_time_minutes": 185.8,
  "pytorch_version": "2.10.0+cu128",
  "cuda_version": "12.8",
  "config_overrides": {
    "batch_size": "2 (reduced from configuration default 8 to fit T4 16GB)",
    "eval_freq": "0 (disabled -- eval envs consume 5.6GB VRAM at startup)"
  },
  "notes": "Baseline run. VLM backbone frozen, only action expert (~100M params) trains."
}